<form method="post" action="{{ path('user_has_sn_delete', {'iduserHasSn': user_has_sn.iduserHasSn}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_has_sn.iduserHasSn) }}">
    <button class="btn">Delete</button>
</form>
