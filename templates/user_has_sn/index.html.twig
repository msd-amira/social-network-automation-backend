{% extends 'base.html.twig' %}

{% block title %}UserHasSn index{% endblock %}

{% block body %}
    <h1>UserHasSn index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IduserHasSn</th>
                <th>AccessToken</th>
                <th>Name</th>
                <th>Lastname</th>
                <th>Photo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_has_sn in user_has_sns %}
            <tr>
                <td>{{ user_has_sn.iduserHasSn }}</td>
                <td>{{ user_has_sn.accessToken }}</td>
                <td>{{ user_has_sn.name }}</td>
                <td>{{ user_has_sn.lastname }}</td>
                <td>{{ user_has_sn.photo }}</td>
                <td>
                    <a href="{{ path('user_has_sn_show', {'iduserHasSn': user_has_sn.iduserHasSn}) }}">show</a>
                    <a href="{{ path('user_has_sn_edit', {'iduserHasSn': user_has_sn.iduserHasSn}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('user_has_sn_new') }}">Create new</a>
{% endblock %}
